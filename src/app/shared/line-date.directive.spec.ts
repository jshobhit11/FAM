import { LineDateDirective } from './line-date.directive';

describe('LineDateDirective', () => {
  it('should create an instance', () => {
    const directive = new LineDateDirective();
    expect(directive).toBeTruthy();
  });
});
