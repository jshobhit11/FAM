import { DateToDirective } from './date-to.directive';

describe('DateToDirective', () => {
  it('should create an instance', () => {
    const directive = new DateToDirective();
    expect(directive).toBeTruthy();
  });
});
