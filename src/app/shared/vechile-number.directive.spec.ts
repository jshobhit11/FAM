import { VechileNumberDirective } from './vechile-number.directive';

describe('VechileNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new VechileNumberDirective();
    expect(directive).toBeTruthy();
  });
});
