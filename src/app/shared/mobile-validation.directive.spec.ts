import { MobileValidationDirective } from './mobile-validation.directive';

describe('MobileValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new MobileValidationDirective();
    expect(directive).toBeTruthy();
  });
});
