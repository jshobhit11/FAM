import { PortingDatesDirective } from './porting-dates.directive';

describe('PortingDatesDirective', () => {
  it('should create an instance', () => {
    const directive = new PortingDatesDirective();
    expect(directive).toBeTruthy();
  });
});
